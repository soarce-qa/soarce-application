version: '3.3'

services:
    app-soarce:
        networks:
            soarcedemo_default:
    redis-soarce:
        networks:
            soarcedemo_default:

networks:
    soarcedemo_default:
        external: true
